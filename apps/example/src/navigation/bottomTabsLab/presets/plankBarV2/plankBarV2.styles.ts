import type {BottomTabNavigationOptions} from '@react-navigation/bottom-tabs';

/**
 * Shared screen options for the Plank Bar V2 tab navigator.
 */
export const plankBarV2TabNavigatorScreenOptions: BottomTabNavigationOptions = {
  headerShown: false,
};
